#include <iostream>
#include<list>

using namespace std;

/*
//function given custom list implementation
// cars in a track at different speeds approach

bool hasCycle(Node<T>* node) {
	Node<T>* fast = head.next;
	Node<T>* slow = head;

	while (fast != NULL && fast.next != NULL && slow != NULL) {
		if (fast == slow) return true;
		fast = fast->next->next;
		slow = slow->next;
	}
	return false;
}

//implementation for standard c++ list

bool hasCycle2(list<int> l) {
	list<int>::iterator fast, slow;
	fast = ++l.begin();
	slow = l.begin();

	while (fast.operator!= NULL)

}
*/